import { Border2Directive } from './border2.directive';

describe('Border2Directive', () => {
  it('should create an instance', () => {
    const directive = new Border2Directive();
    expect(directive).toBeTruthy();
  });
});
